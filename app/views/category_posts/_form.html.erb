<%= form_for(@category_post) do |f| %>
  <% if @category_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_post.errors.count, "error") %> prohibited this category_post from being saved:</h2>

      <ul>
      <% @category_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br />
    <%= f.text_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
